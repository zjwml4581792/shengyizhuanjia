import { PhoneCheckDirective } from './phone-check.directive';

describe('PhoneCheckDirective', () => {
  it('should create an instance', () => {
    const directive = new PhoneCheckDirective();
    expect(directive).toBeTruthy();
  });
});
